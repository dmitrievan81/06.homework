#include "array.hpp"
#include <cstddef>

int main(){
    
    // 1. 
    Array<int> arr (1);
    
    arr.push_back(1);
    arr.push_back(1);
    arr.push_back(1);


    //std::size_t idx = 0;
   
    // 2. 
    //for(; idx < 10; idx++)
    //    arr.push_back(static_cast<int>(idx));
    // 3.
    // 4.
    // print std::size_t sz1 = arr.size();
    // 5.
    //arr.erase(3);
    // 6.
    // 7.
    // arr.insert(arr.begin(), 10);
    // 8.
    // 9.
    // std::size_t middle = arr.size() / 2 - 1;
    // arr.insert(middle, 10);
    // 10.
    // arr.push_back(30);
    // 11.
    // 12.





    return 0;
}